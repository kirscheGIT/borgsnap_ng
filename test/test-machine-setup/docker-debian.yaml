# Lima instance: docker-dev
# Debian 13 (Trixie) + Docker CE, for running the borgsnap_ng mock test
# harness (and any other container-based work) without polluting the Mac.
#
# IMPORTANT - do not use `limactl start --name=docker-dev docker-debian.yaml`
# directly. Every Lima template - including the bare _images/debian-13 used
# as `base:` below - ultimately chains back to templates/default.yaml, which
# adds an unconditional, un-suppressible
#   - location: "~"
# mount (your whole macOS home, mirrored 1:1 as /Users/<you> in the guest,
# since it has no mountPoint override). Lima combines mount lists across the
# whole base chain rather than replacing them, so this entry cannot be
# removed declaratively from this file. Use the wrapper script instead,
# which creates the instance, strips that one entry from the *resolved*
# config, and only then starts it (a one-time fix - stop/start afterwards
# never re-merges the base templates):
#
#   ./create-clean-instance.sh docker-dev docker-debian.yaml
#
# Usage after that:
#   limactl shell docker-dev
#   # or from the Mac host directly:
#   export DOCKER_HOST=$(limactl list docker-dev --format 'unix://{{.Dir}}/sock/docker.sock')
#   docker run --rm hello-world
#
minimumLimaVersion: 2.0.0

base:
  - template:_images/debian-13

# containerd is managed by Docker itself, not by Lima.
containerd:
  system: false
  user: false

cpus: 4
memory: "4GiB"
disk: "40GiB"

# Adjust the host-side path to wherever you keep the repo checkout — or,
# better, don't: leave the placeholder below and let
# create-clean-instance.sh substitute it automatically (auto-detected via
# git, an env var, or an interactive prompt). See that script for details.
mounts:
  - location: "__BORGSNAP_REPO_PATH__"
    mountPoint: "/home/{{.User}}/borgsnap_ng"
    writable: true

provision:
  # Required for host.docker.internal to resolve from inside containers.
  - mode: system
    script: |
      #!/bin/sh
      set -eux
      sed -i 's/host.lima.internal.*/host.lima.internal host.docker.internal/' /etc/hosts

  - mode: system
    script: |
      #!/bin/bash
      set -eux -o pipefail
      command -v docker >/dev/null 2>&1 && exit 0
      export DEBIAN_FRONTEND=noninteractive
      apt-get update
      apt-get install -y --no-install-recommends ca-certificates curl gnupg
      install -m 0755 -d /etc/apt/keyrings
      curl -fsSL https://download.docker.com/linux/debian/gpg -o /etc/apt/keyrings/docker.asc
      chmod a+r /etc/apt/keyrings/docker.asc
      . /etc/os-release
      echo \
        "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/debian ${VERSION_CODENAME} stable" \
        > /etc/apt/sources.list.d/docker.list
      apt-get update
      apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
      systemctl enable --now docker
      groupadd -f docker
      usermod -aG docker "{{.User}}"

probes:
  - script: |
      #!/bin/bash
      set -eux -o pipefail
      if ! timeout 60s bash -c 'until command -v docker >/dev/null 2>&1 && docker info >/dev/null 2>&1; do sleep 2; done'; then
        echo "docker is not running yet"
        exit 1
      fi
    hint: |
      The docker installation or docker.service startup is taking too long.
      Run "limactl shell docker-dev sudo journalctl -u docker" to check.

portForwards:
  - guestSocket: "/var/run/docker.sock"
    hostSocket: "{{.Dir}}/sock/docker.sock"

message: |
  To run Docker CLI commands from the Mac host, run:
    export DOCKER_HOST="unix://{{.Dir}}/sock/docker.sock"
    docker run --rm hello-world

  Or just enter the VM directly:
    limactl shell docker-dev
